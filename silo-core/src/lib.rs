#![deny(missing_docs)]

//! Silo's core crate, containing an actor system and logging.

/// The internal logging module.
mod logging;

/// The service module.
pub mod service;

/// The models module.
pub mod models;
